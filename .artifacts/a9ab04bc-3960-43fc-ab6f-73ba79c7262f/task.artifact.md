# Task - Implement Sync DTO Mapping Layer

- [ ] Create `farmer_sync_dtos.dart` with mapping logic for Farmer, Farm, and DamageReport.
- [ ] Update `RemoteFarmerRepository` to use `FarmerSyncDto`.
- [ ] Update `RemoteFarmRepository` to use `FarmSyncDto`.
- [ ] Update `RemoteDamageReportRepository` to use `DamageReportSyncDto`.
- [ ] Verify payload normalization (Date formatting, Gender validation).
- [ ] Run `flutter analyze` and tests.
- [ ] Update `PROJECT_STATUS.md` and `CHANGELOG.md`.
- [ ] Commit and push to `farmers` branch.
