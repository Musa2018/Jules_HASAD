# General
.DS_Store
*.log
*.swp
*.bak
test_results.txt
test_output.txt

# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
hasad/mobile/ios/Flutter/Generated.xcconfig
hasad/mobile/ios/Flutter/flutter_export_environment.sh
hasad/mobile/ios/Flutter/flutter_podhelper.rb
hasad/mobile/ios/Flutter/App.framework
hasad/mobile/ios/Flutter/Flutter.framework
hasad/mobile/ios/Flutter/Flutter.podspec
hasad/mobile/android/app/src/main/java/io/flutter/plugins/
hasad/mobile/lib/generated/

# Drift & Freezed & Riverpod generated files (Keep them if you want them in the repo, but usually they are ignored)
# For this project, they seem to be needed for CI since build_runner might not run there (but it should).
# The user said the CI failed due to missing files before, so maybe we should NOT ignore them if CI doesn't generate them.
# However, standard practice is to ignore them.
# hasad/mobile/lib/**/*.freezed.dart
# hasad/mobile/lib/**/*.g.dart

# .NET
[Bb]in/
[Oo]bj/
*.user
*.userosscache
*.sln.docstates
.vs/
.idea/
.vscode/
*.suo
*.user
*.userosscache
*.sln.docstates

# Secrets
appsettings.Development.json
secrets.json
.env

# Infrastructure
.terraform/
*.tfstate
*.tfstate.backup
